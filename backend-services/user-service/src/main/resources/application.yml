server:
  port: 8081
spring:
  application:
    name: user-service-1
  cloud:
    nacos:
      discovery:
        server-addr: 54.219.180.170:8848
  datasource:
    url: jdbc:mysql://54.219.180.170:3306/ai_qa_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ai_qa_system
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

# SpringDoc Swagger配置
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: method
  packages-to-scan: com.ai.qa.user.api.controller

# JWT配置
jwt:
  secret: ai-qa-system-secret-key-for-jwt-token-generation-change-this-in-production
  expiration: 86400000 # 24小时，单位毫秒