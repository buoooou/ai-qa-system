server:
  port: 8081
spring:
  application:
    name: user-service-yulong
  # 服务发现配置
  cloud:
    nacos:
      discovery:
        server-addr: 3.84.225.222:8848
        username: nacos
        password: nacos
        register-enabled: false  # 先禁用注册，稍后再启用
        failure-tolerance-enabled: true
  datasource:
    url: jdbc:mysql://3.84.225.222:3306/ai_qa_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

# SpringDoc配置
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    url: /api/user/v3/api-docs
    config-url: /api/user/v3/api-docs/swagger-config
    disable-swagger-default-url: true
