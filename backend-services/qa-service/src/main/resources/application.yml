server:
  port: 8082

spring:
  application:
    name: qa-service-zj
  main:
    allow-bean-definition-overriding: true

  # MySQL 数据库配置
  datasource:
    url: jdbc:mysql://mysql3.sqlpub.com:3308/airline_order_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: airline_lhy
    password: JxC55d1F7NBBl7aO
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  # 启用Nacos服务注册发现
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: localhost:8848
        username: nacos
        password: nacos
        namespace: public

# Google Gemini AI 配置
chat:
  api:
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent
    key: AIzaSyBErgAI6VJIhz3G9DN14aBHxYW7y-EiU2w

# 日志配置
logging:
  level:
    com.ai.qa.service: DEBUG
    org.springframework.web: INFO
    com.alibaba.cloud: INFO
    com.alibaba.nacos: INFO
    com.ai.qa.service.client: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# 分页配置
app:
  pagination:
    default-page-size: 10
    max-page-size: 100
