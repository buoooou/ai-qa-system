spring:
  application:
    name: api-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:localhost:8848}
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: ${NACOS_SERVER:localhost:8848}
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
        enabled: false
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}